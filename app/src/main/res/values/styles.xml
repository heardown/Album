<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>


    <style name="AlertDialogTheme" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:background">@android:color/transparent</item>
    </style>

    <style name="TransColorTheme" parent="AppTheme">

    </style>

    <style name="TransImageTheme" parent="AppTheme">
        <!--在Android 4.4之前的版本上运行，直接跟随系统主题-->
    </style>

    <style name="SwipeTheme" parent="TransColorTheme">
        <!--  //背景透明，不设滑动关闭时背景就是黑的。-->
        <item name="android:windowIsTranslucent">true</item>
        <!--//Activity右滑进出的动画，觉得这个不好看随便换成自己的-->
        <item name="android:windowAnimationStyle">@style/SlideRightAnimation</item>
    </style>

    <style name="SwipeNormalTheme" parent="AppTheme">
        <!--  //背景透明，不设滑动关闭时背景就是黑的。-->
        <item name="android:windowIsTranslucent">true</item>
        <!--//Activity右滑进出的动画，觉得这个不好看随便换成自己的-->
        <item name="android:windowAnimationStyle">@style/SlideRightAnimation</item>
    </style>

</resources>
